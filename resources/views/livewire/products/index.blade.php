<x-table
        :columns="$columns"
        :actions="$actions"
        :rows="$products"
        :perPageOptions="$perPageOptions"
        :sortDirection="$sortDirection"
        :filters="$filters"/>
